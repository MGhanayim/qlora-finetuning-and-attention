# Part 2 — attention from scratch (runs locally on Mac CPU)
torch>=2.2
matplotlib>=3.8
seaborn>=0.13
numpy>=1.26

# Notebook runtime
jupyter>=1.0
ipykernel>=6.29

# Part 1 dependencies are added later when we get to fine-tuning (Colab):
#   transformers, peft, trl, bitsandbytes, datasets, accelerate, pandas, openai, python-dotenv
